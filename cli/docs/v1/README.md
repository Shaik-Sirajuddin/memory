# CLI Docs v1

This folder keeps minimal release/change documentation for the `cli` package.

- `changeset-format.md`: official Changesets file format and local usage.
- `changes.md`: short history of notable `cli` changes.

