# CLI Changes (v1)

## 2026-03-31

- Initialized TypeScript Node CLI in `cli/` with `mem` binary.
- Added commands: `init`, `agent init <name>`, and `help`.
- Added tests for command flow, `.memory` discovery, worktree logic, and agent setup.
- Added minimal v1 docs under `cli/docs/v1/`.

