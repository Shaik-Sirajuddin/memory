{
  "name": "mem",
  "version": "0.1.0",
  "description": "Memory bootstrap CLI",
  "type": "module",
  "bin": {
    "mem": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "cosmiconfig": "^8.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.14.1",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
